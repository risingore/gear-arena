# Cloudflare Workers + Static Assets (Vite dist) → machines.risingore.workers.dev / custom domain
name = "machines"
compatibility_date = "2026-01-01"
preview_urls = false

[assets]
directory = "dist"
not_found_handling = "single-page-application"
